services:
  mock-gateway:
    build:
      context: ./mock-gateway
    environment:
      PORT: 8787
      STAGE: dev
      STRICT_COMPATIBILITY_MODE: "true"
      ROUTE_INTEGRATIONS_JSON: '{}'
    ports:
      - "8787:8787"
